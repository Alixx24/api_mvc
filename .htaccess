RewriteEngine On

# اگر فایل یا دایرکتوری وجود داشت، مستقیم سرو کن
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# همه درخواست‌ها رو به index.php بفرست (با حفظ query string)
RewriteRule ^ index.php [QSA,L]
